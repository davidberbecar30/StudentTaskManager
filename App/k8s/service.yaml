apiVersion: v1
kind: Service
metadata:
  name: student-task-manager-service
  labels:
    app: student-task-manager
spec:
  type: NodePort
  selector:
    app: student-task-manager
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080